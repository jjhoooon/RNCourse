import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const GameScreen = () => {
    return (
        <View>
            <Text>Here is a GameScreen!</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default GameScreen;
